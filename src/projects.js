import projects from './projects.json' with {type: "json"};

const chess = projects.chess;
const breathalyzer = projects.breathalyzer;
const blackjack = projects.blackjack;
const calculator = projects.calculator;
const minesweeper = projects.minesweeper;
const pHsensor = projects.pHsensor;
const portfolio = projects.portfolio;

export const projectParam = [chess, breathalyzer, blackjack, calculator, minesweeper, pHsensor, portfolio];